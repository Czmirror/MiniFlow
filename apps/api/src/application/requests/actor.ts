/**
 * Authentication is not wired yet, so workflow actions use a fixed actor.
 * Replace this constant from an auth/session source instead of widening HTTP bodies.
 */
export const DEFAULT_ACTOR_ID = "00000000-0000-0000-0000-000000000001";
